(function(){
      const u = document.getElementById("id_username");
      const p = document.getElementById("id_password");
      if (u) u.setAttribute("placeholder", "Your username");
      if (p) p.setAttribute("placeholder", "Your password");
    })();